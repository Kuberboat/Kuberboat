kind: Pod
metadata:
  name: test-pod
  labels:
    app: mypod
    env: dev
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports: 
        - 80
      resources:
        cpu: 1
        memory: 102400000
